We now have running components for the ui, server, and docs. They build on GH Actions CI and deploy to Railway. It's time to make a plan for the important part - building what's necessary for this project to start to provide its stated value in the README and specs. When Aileron runs, it needs to hook into an existing agent and act as its control plane. Make recommendations for how we start to do this in either OpenClaw or Claude Code. What are some good user flows to hook up first so we may start putting together the policy engine and approval orchestrator for one fo the MVP use cases: Calendar, Git/GitHub, Payments/Stripe.

---

Have you made unit tests and integration tests for all newly created features and functions?

---

continue

---

Make a note to always create unit and integration tests if applicable for new features, bug fixes, etc. Also: update the README documentation if appropriate.

---

Keep the version at 0.0.1, 0.0.2 until I say we'll move to a minor version increment. And update the health check output in the README; it has a timestamp on it now.

---

Note that you may increment the patch number of the release, for instance 0.0.x, until I say we must increment the minor number (eg 0.x.0). Is that clear?