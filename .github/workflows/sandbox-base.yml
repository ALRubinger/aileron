name: Sandbox Base Image

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/sandbox-base.yml"
      - "images/sandbox-base/**"
      # The published image now bakes aileron-mcp (issue #957), so a change to
      # the binary's source must rebuild the image on PR. internal/** is broad
      # but correct: aileron-mcp links the internal module.
      - "cmd/aileron-mcp/**"
      - "internal/**"
      - "sdk/go/**"
      - "go.work"
      - "go.work.sum"
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: sandbox-base-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/alrubinger/aileron-sandbox-base

jobs:
  build:
    name: Build sandbox-base
    runs-on: ubuntu-latest
    # A throwaway local registry the PR compose path pushes the substrate to and
    # FROMs. The stage-2 `devcontainer build` runs under the buildx
    # docker-container driver (an isolated BuildKit container), which cannot see
    # images in the local docker store. On publish the substrate lives in GHCR so
    # the driver can pull it; the PR path now mirrors that through localhost:5000
    # instead of a `load`ed image the driver can't reach (the #1342 regression
    # reverted in #1348). The service is idle on tag/dispatch publish builds.
    services:
      registry:
        image: registry:3
        ports:
          - 5000:5000
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-qemu-action@v4

      # network=host lets the docker-container builder reach the localhost:5000
      # registry service (BuildKit treats localhost registries as insecure/http
      # automatically), so it can both push and FROM the PR substrate.
      - uses: docker/setup-buildx-action@v4
        with:
          driver-opts: network=host

      # Version stamped into the baked aileron-mcp and the
      # ai.aileron.mcp.version image label. Tag builds carry the release
      # version (v-prefix stripped to match version.Version); PR and dispatch
      # builds use "dev", consistent with the host CLI's default version.
      - name: Resolve build version
        id: ver
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=dev" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=sha,prefix=git-
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=edge,enable=${{ github.event_name == 'workflow_dispatch' }}
          labels: |
            org.opencontainers.image.title=Aileron sandbox base
            org.opencontainers.image.description=Minimal sandbox substrate for Aileron-managed agent containers.

      - name: Log in to GitHub Container Registry
        if: ${{ startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch' }}
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve publish flag
        id: publish
        run: echo "publish=${{ startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch' }}" >> "$GITHUB_OUTPUT"

      # Node + @devcontainers/cli, pinned to match ci.yml and
      # sandbox-features.yml so the three workflows stay aligned (umbrella #1319).
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
      - name: Install @devcontainers/cli
        run: npm install -g @devcontainers/cli@0.87.0

      # Stage 1 (substrate): build the repo-root, multi-arch, mcp-baked substrate
      # exactly as before, minus github-cli (gh now ships as a Feature, composed
      # in stage 2). context is the repo root so the mcp-builder stage can compile
      # aileron-mcp from go.work + source. On publish the multi-arch substrate is
      # pushed to a short-lived per-sha GHCR ref that the compose stage FROMs; on
      # PR it is pushed single-arch to the localhost:5000 registry service the
      # compose stage FROMs (the docker-container driver can pull a registry but
      # not a `load`ed local image).
      - name: Build substrate (publish, multi-arch)
        if: ${{ steps.publish.outputs.publish == 'true' }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: images/sandbox-base/Containerfile.published
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.IMAGE_NAME }}:substrate-${{ github.sha }}
          build-args: |
            AILERON_VERSION=${{ steps.ver.outputs.version }}
            AILERON_COMMIT=${{ github.sha }}

      - name: Build substrate (PR, single-arch push to local registry)
        if: ${{ steps.publish.outputs.publish != 'true' }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: images/sandbox-base/Containerfile.published
          push: true
          tags: localhost:5000/aileron-sandbox-base-substrate:pr
          build-args: |
            AILERON_VERSION=${{ steps.ver.outputs.version }}
            AILERON_COMMIT=${{ github.sha }}

      # Stage 2 (compose): render a scratch devcontainer workspace that FROMs the
      # substrate and composes the gh Feature on top, then build it with
      # @devcontainers/cli. The compose installs gh and emits the
      # devcontainer.metadata label carrying gh's customizations.aileron.cli unit
      # (the unit loader, #1322, reads it). renderWorkspace mirrors the umbrella's
      # verified probe: a .devcontainer/devcontainer.json from the committed
      # template plus a copy of images/sandbox-features/gh.
      - name: Compose gh + publish multi-arch manifest
        run: |
          set -euo pipefail

          renderWorkspace() {
            local ws="$1" substrate="$2"
            rm -rf "$ws" && mkdir -p "$ws/.devcontainer"
            cp -r images/sandbox-features/gh "$ws/.devcontainer/gh"
            SUBSTRATE_IMAGE="$substrate" envsubst < images/sandbox-base/compose/devcontainer.json.tmpl \
              > "$ws/.devcontainer/devcontainer.json"
          }

          if [ "${{ steps.publish.outputs.publish }}" = "true" ]; then
            SUBSTRATE="${IMAGE_NAME}:substrate-${{ github.sha }}"
            DIGESTS=()
            for arch in amd64 arm64; do
              renderWorkspace "compose-$arch" "$SUBSTRATE"
              FINAL="${IMAGE_NAME}:compose-${{ github.sha }}-$arch"
              devcontainer build \
                --workspace-folder "compose-$arch" \
                --platform "linux/$arch" \
                --image-name "$FINAL"
              docker push "$FINAL"
              DIGESTS+=("${IMAGE_NAME}@$(docker buildx imagetools inspect "$FINAL" --format '{{.Manifest.Digest}}')")
            done
            # Assemble the published multi-arch manifest under every meta tag.
            while IFS= read -r tag; do
              [ -n "$tag" ] || continue
              docker buildx imagetools create --tag "$tag" "${DIGESTS[@]}"
            done <<< "${{ steps.meta.outputs.tags }}"
          else
            # PR: compose single-arch onto the substrate pulled from the local
            # registry (same docker-container driver as publish) for smoke only.
            # devcontainer build loads the result into the docker store so the
            # smoke step can `docker run` it.
            renderWorkspace compose-pr localhost:5000/aileron-sandbox-base-substrate:pr
            devcontainer build \
              --workspace-folder compose-pr \
              --image-name aileron-sandbox-base:smoke
          fi

      # Single-arch local load of the Tier 0 Containerfile so the smoke step can
      # `docker run` it. This recipe takes the images/sandbox-base context (no
      # repo-root, no AILERON_VERSION/COMMIT build-args) and is UNBAKED (no
      # aileron-mcp). It still installs gh via apk (the local Tier 0
      # Feature-compose move is #1321b), so it remains the #1151 P1 gate on the
      # local recipe.
      - name: Build local Tier 0 image for smoke test
        if: ${{ steps.publish.outputs.publish != 'true' }}
        uses: docker/build-push-action@v7
        with:
          context: images/sandbox-base
          file: images/sandbox-base/Containerfile
          load: true
          tags: aileron-sandbox-base-local:smoke

      # Image-fidelity smoke against the composed published image (PR only, where
      # the composed image is loaded as :smoke): gh is present via the Feature AND
      # the devcontainer.metadata label carries gh's CLI unit, plus the bash
      # substrate, the baked-MCP invariants (#957), and the #959 invariants.
      - name: Smoke test image
        if: ${{ steps.publish.outputs.publish != 'true' }}
        run: |
          set -euo pipefail

          # gh present on both the composed published image (via the Feature) and
          # the local Tier 0 image (still apk, until #1321b). set -e fails the job
          # on a non-zero `gh --version`.
          assertGhVersion() {
            local img="$1"
            echo "asserting gh --version on $img"
            docker run --rm "$img" gh --version
          }
          assertGhVersion aileron-sandbox-base-local:smoke
          assertGhVersion aileron-sandbox-base:smoke

          # The composed image carries gh's CLI-capability unit in the
          # devcontainer.metadata label (the #1322 loader's source of truth).
          meta=$(docker inspect --format '{{ index .Config.Labels "devcontainer.metadata" }}' aileron-sandbox-base:smoke)
          if ! printf '%s' "$meta" | grep -q '"key":"user/github"'; then
            echo "devcontainer.metadata label missing gh's customizations.aileron.cli unit"
            echo "$meta"
            exit 1
          fi
          echo "devcontainer.metadata carries gh unit"

          IMG=aileron-sandbox-base:smoke
          docker run --rm "$IMG" bash -c 'echo hi' | grep -q '^hi$'
          bash_path=$(docker run --rm "$IMG" which bash)
          if [ "$bash_path" != "/bin/bash" ]; then
            echo "expected which bash to resolve to /bin/bash, got $bash_path"
            exit 1
          fi
          docker run --rm "$IMG" sh -c '[ ! -d /etc/aileron/shell ]'

          # Baked aileron-mcp runs and reports a non-empty version.
          mcp_version=$(docker run --rm "$IMG" aileron-mcp --version)
          if [ -z "$mcp_version" ]; then
            echo "baked aileron-mcp --version returned empty"
            exit 1
          fi
          echo "baked aileron-mcp --version: $mcp_version"

          # The ai.aileron.mcp.version label is present (launcher detection key).
          label=$(docker inspect --format '{{ index .Config.Labels "ai.aileron.mcp.version" }}' "$IMG")
          if [ -z "$label" ]; then
            echo "ai.aileron.mcp.version label missing on smoke image"
            exit 1
          fi
          echo "ai.aileron.mcp.version label: $label"

          # #959: the tools.txt manifest and its profile banner are gone.
          docker run --rm "$IMG" sh -c '[ ! -e /etc/aileron/tools.txt ]'
          docker run --rm "$IMG" sh -c '[ ! -e /etc/profile.d/aileron-tools.sh ]'
          login_out=$(docker run --rm "$IMG" bash -lc 'true' 2>&1 || true)
          if printf '%s' "$login_out" | grep -qi 'tools manifest'; then
            echo "login shell still prints a stale tools manifest banner: $login_out"
            exit 1
          fi
          # The proxy CA path must survive the tools.txt removal: the
          # directory exists and the installer helper is present so a
          # bootstrap launch can write /etc/aileron/proxy/ca.pem.
          docker run --rm "$IMG" sh -c '[ -d /etc/aileron/proxy ]'
          docker run --rm "$IMG" sh -c '[ -x /usr/local/bin/aileron-install-proxy-ca ]'
          echo "sandbox-base smoke test passed"
