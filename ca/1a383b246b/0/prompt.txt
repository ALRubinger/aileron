I deleted the "pr-6" environment. The CI job passes. The Deploy Preview job fails with:  ENV_NAME="pr-6"
  
  # Create environment by duplicating production (idempotent — ignores error if it exists)
  railway environment new "$ENV_NAME" --duplicate production 2>/dev/null || true
  
  # Deploy server (repo root as context — Dockerfile references go.work, core/, sdk/go/)
  railway up --service server --environment "$ENV_NAME" --detach --project "$RAILWAY_PROJECT_ID" .
  
  # Deploy UI (ui/ subdirectory as context)
  railway up --service ui --environment "$ENV_NAME" --detach --project "$RAILWAY_PROJECT_ID" ./ui
  
  # Capture the public UI URL
  UI_URL=$(railway domain --service ui --environment "$ENV_NAME" 2>/dev/null || echo "pending")
  echo "ui_url=$UI_URL" >> "$GITHUB_OUTPUT"
  shell: /usr/bin/bash -e {0}
  env:
    RAILWAY_TOKEN: ***
    RAILWAY_PROJECT_ID: ***
> Environment name pr-6
> Duplicate from production
Indexing...
prefix not found

---

Yes

---

I don't see a "Source" field in the Railway project settings for Aileron project

---

Connect repos. Configured services. Enabled PR environments. Where do I set the deploy trigger?

---

There is a "Deploy" section but no branch setting.

---

I removed the 3 GitHub Acrions secrets. The CI workflow is passing. Is that the only workflow now for GitHub Actions?

---

May I remove the token I created in Railway then?

---

I removed the Railway token from the Aileron project

---

There are some warnings on the CI job we should clean up. Here's one:

Node.js 20 actions are deprecated. The following actions are running on Node.js 20 and may not work as expected: actions/checkout@v4, actions/setup-go@v5, actions/setup-node@v4, pnpm/action-setup@v4. Actions will be forced to run with Node.js 24 by default starting June 2nd, 2026. Node.js 20 will be removed from the runner on September 16th, 2026. Please check if updated versions of these actions are available that support Node.js 24. To opt into Node.js 24 now, set the FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true environment variable on the runner or in your workflow file. Once Node.js 24 becomes the default, you can temporarily opt out by setting ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION=true. For more information see: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/

---

Yes; Lint: Restore cache failed: Some specified paths were not resolved, unable to cache dependencies.