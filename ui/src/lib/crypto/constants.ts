/** Known plaintext used for passphrase verification. Not secret. */
export const VERIFICATION_CONSTANT = 'aileron-kek-verification-ok';

/** Length of the Argon2id salt in bytes. */
export const SALT_LENGTH = 16;
